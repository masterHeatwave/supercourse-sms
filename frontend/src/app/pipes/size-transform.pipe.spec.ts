import { SizeTransformPipe } from './size-transform.pipe';

describe('SizeTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
